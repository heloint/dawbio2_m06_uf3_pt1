import { ValidatePasswordConfirmationDirective } from './validate-password-confirmation.directive';

describe('ValidatePasswordConfirmationDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatePasswordConfirmationDirective();
    expect(directive).toBeTruthy();
  });
});
